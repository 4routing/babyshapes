workflows:
  android-debug:
    name: BabyShapes APK
    max_build_duration: 30
    environment:
      java: 17
    scripts:
      - name: Build APK
        script: |
          gradle assembleDebug
    artifacts:
      - app/build/outputs/apk/debug/*.apk
